---
label: Note
hide_body: false
fields:
- type: text
  name: title
  label: Title
  config:
    required: true
- name: subtitle
  type: text
  config:
    required: false
  label: Subtitle
- type: datetime
  name: date
  label: Publish Date
  config:
    date_format: MM-DD-YYYY
    export_format: MM-DD-YYYY
    required: true
  default: now
- type: datetime
  name: updated
  label: Updated Date
  config:
    date_format: MM-DD-YYYY
    export_format: MM-DD-YYYY
    required: true
  default: now
- type: list
  name: tags
  label: Tags
  config:
    use_select: false
    min: 
